---
  # user ubuntu with sudo need for first run on aws
- hosts: all
  remote_user: root #ubuntu
  #sudo: yes

  vars_files:
    - vars/main.yml
    #- vars/private.yml
    #- vars/crypted.yml

  roles:
    # required by webserver
    - common
    #- dns
    - apache-php
    - phpmyadmin
    - mysql
    - nginx
    - backups
    - server-scripts
    - exim4
    - docker
    - locales
    - memcached
    - ufw

    # monitoring
    - diamond
    #- bind9
    #- docker-grafana
    #- docker-graphite
    - monit

    # other
    - ansible
